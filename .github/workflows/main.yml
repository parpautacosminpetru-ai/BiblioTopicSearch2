name: Build Lupa Semantica APK

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Unzip project
        run: |
          set -euo pipefail
          rm -rf project
          mkdir -p project
          unzip -q BiblioTopicSearch_FINAL.zip -d project
          test -d project/app
          test -d semantic-final/app

      - name: Apply semantic final overlay
        run: |
          set -euo pipefail
          rsync -a semantic-final/app/ project/app/
          cp semantic-final/MODEL_NOTICE.md project/MODEL_NOTICE.md
          test -f project/app/src/main/java/ro/bibliotopicsearch/app/semantic/SemanticAnalyzer.java
          grep -q 'versionName = "2.2.0"' project/app/build.gradle.kts

      - name: Install pinned semantic model assets
        env:
          MODEL_REV: acc85025f9078147bcc1ce55a4ddb9ec5e0d87cf
        run: |
          set -euo pipefail
          ASSET_DIR=project/app/src/main/assets/semantic
          BASE="https://huggingface.co/sentence-transformers/distiluse-base-multilingual-cased-v2/resolve/${MODEL_REV}"
          mkdir -p "$ASSET_DIR"
          curl -fL --retry 4 --retry-delay 3 --connect-timeout 30 "$BASE/onnx/model_qint8_arm64.onnx?download=true" -o "$ASSET_DIR/semantic_model.onnx"
          curl -fL --retry 4 --retry-delay 3 --connect-timeout 30 "$BASE/vocab.txt?download=true" -o "$ASSET_DIR/vocab.txt"
          curl -fL --retry 4 --retry-delay 3 --connect-timeout 30 "$BASE/2_Dense/model.safetensors?download=true" -o "$ASSET_DIR/dense_model.safetensors"
          echo 'c78c64bb0446ceb7086d2de24c2f4297e7a909eb457b4f68e5fdb6ab19f285c0  project/app/src/main/assets/semantic/semantic_model.onnx' | sha256sum -c -
          echo '0a21b1ce908e772ebf09f93c20ca09524c32706e9918d9c0169a3f0663b191ed  project/app/src/main/assets/semantic/dense_model.safetensors' | sha256sum -c -
          test -s "$ASSET_DIR/vocab.txt"
          du -h "$ASSET_DIR"/*

      - name: Upload patched source without model
        uses: actions/upload-artifact@v4
        with:
          name: Lupa-Semantica-2.2-SOURCE
          path: |
            project
            !project/app/src/main/assets/semantic/semantic_model.onnx
            !project/app/src/main/assets/semantic/dense_model.safetensors
            !project/app/src/main/assets/semantic/vocab.txt
          if-no-files-found: error

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.13"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK packages
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platforms;android-36" "build-tools;35.0.0"

      - name: Build Debug APK
        run: |
          set -euo pipefail
          gradle --no-daemon -p project :app:assembleDebug

      - name: Verify APK contents
        run: |
          set -euo pipefail
          APK=project/app/build/outputs/apk/debug/app-debug.apk
          test -s "$APK"
          unzip -l "$APK" | grep -q 'assets/semantic/semantic_model.onnx'
          unzip -l "$APK" | grep -q 'assets/semantic/vocab.txt'
          unzip -l "$APK" | grep -q 'assets/semantic/dense_model.safetensors'
          ls -lh "$APK"

      - name: Prepare APK
        run: |
          mkdir -p output
          cp project/app/build/outputs/apk/debug/app-debug.apk output/Lupa-Semantica-2.2.apk
          sha256sum output/Lupa-Semantica-2.2.apk | tee output/SHA256SUMS.txt

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Lupa-Semantica-2.2-APK
          path: output
          if-no-files-found: error
